# Only needed for production builds where the frontend and backend are on
# different hosts (e.g. Vercel + Railway). Local dev via `npm run dev` uses
# vite.config.ts's proxy instead and needs nothing here.
VITE_API_BASE_URL=https://your-backend-host.example.com/api
